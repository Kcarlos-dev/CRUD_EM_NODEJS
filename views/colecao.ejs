<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>coleção</title>
</head>
<body>
    <a href="/">Cria um novo post</a>
    <h1>Suas postagens</h1>
    <ul>
        <% posts.forEach((post)=>{%>
            <li>
                <h2><%= post.titulo %></h2>
                <p><%= post.conteudo %></p>
            </li>
            <a href="/editar?n=<%= post.id %>"><button>Editar</button></a>
            <a id="deletar" href="/delete/<%= post.id %>"><button >Deletar</button></a>
        <% }) %>
    </ul>
<script src="script/index.js"></script>
</body>
</html>